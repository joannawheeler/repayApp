import React, { Component } from 'react';

class FinishedAddingItemsButton extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <div>
        <button onClick="">I am finished adding items</button>
      </div>
    );
  }
}

export default FinishedAddingItemsButton;
